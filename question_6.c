#include <stdio.h>

int distinct_numbers(int m) {
  
}

int main() {
  int l, r, k, i, j, m;
  printf("Enter two numbers: ")
  scanf("%d %d", &l, &r);

  if ((l%2 == 0 && r%2 == 0) || (l%2 != 0 && r%2 != 0)) {
    m = (l+r)/2;

  }
}
